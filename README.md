# NeuraWatch
NeuraWatch is a real-time video analytics web app that uses a pre-trained YOLO model to detect and track objects from webcam or uploaded video, trigger polygon zone entry/exit alerts, and store event snapshots for review in a live dashboard.
